import React from 'react'
import ReactDOM from 'react-dom/client'
import AppPoc from './poc/App.poc.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AppPoc />
  </React.StrictMode>,
)
